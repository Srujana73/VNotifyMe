<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.vnotifyme">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.VNotifyMe">
        <activity android:name=".WinnersList"></activity>
        <activity android:name=".EventWinners" />
        <activity android:name=".EventsList" />
        <activity android:name=".RegforEvent" />
        <activity android:name=".ParticipantsList" />
        <activity android:name=".RegisterEvent" />
        <activity android:name=".StudReg" />
        <activity android:name=".EMhome" />
        <activity android:name=".StudHome" />
        <activity android:name=".MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>